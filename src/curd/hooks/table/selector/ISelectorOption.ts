import type { RootEntity } from '../../../../base'
import type { IBaseTableOption } from '../base'

/**
 * # `Selector` 的 `Hook` 可选配置
 * @author Hamm.cn
 */
export interface ISelectorOption<E extends RootEntity> extends IBaseTableOption<E> {
  // TODO 暂无
}
