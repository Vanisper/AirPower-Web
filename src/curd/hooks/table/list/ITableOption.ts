import type { RootEntity } from '../../../../base'
import type { IBaseTableOption } from '../base'

/**
 * # `TableList` 的 `Hook` 可选配置
 * @author Hamm.cn
 */
export interface ITableOption<E extends RootEntity> extends IBaseTableOption<E> {
  // !TODO
}
