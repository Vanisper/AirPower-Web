import type { AirEntity } from '../../../../../spms/airpower/base/AirEntity'
import type { IBaseTableOption } from '../base/IBaseTableOption'

/**
 * # `TableList` 的 `Hook` 可选配置
 * @author Hamm.cn
 */
export interface ITableOption<E extends AirEntity> extends IBaseTableOption<E> {
  // !TODO
}
